diff --git a/src/main.cpp b/src/main.cpp
index d9b7e46..cdd71a9 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -577,10 +577,3 @@ int main(int argc, char *argv[]) {
-#ifdef Q_OS_LINUX
-#ifndef Q_OS_ANDROID
-    if (getuid() && !testPeloton && !testHomeFitnessBudy && !testPowerZonePack && !smokeTest) {
-
-        printf("Runme as root!\n");
-        return -1;
-    } else
-        printf("%s", "OK, you are root.\n");
-#endif
-#endif
+    // Native BlueZ clients use the desktop user's D-Bus permissions. Let the
+    // requested backend report missing permissions instead of requiring the
+    // entire application to run as root (including network-only export).
